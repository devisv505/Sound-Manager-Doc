---
title: 'Arcade in pictures.'
description: 'Unity screenshots of Arcade, with scene states, sound graphs, and steps to try.'
kind: guide
sourceRevision: 746a1cc7d2602bc59df6096eac50508ded6ef49e
sourcePaths:
  - Assets/DEV505/SoundManager/Samples/07-Arcade/Demo.unity
  - Assets/DEV505/SoundManager/Scripts/Samples/Arcade/ArcadeDemo.cs
tags: [screenshots, demos, arcade]
---

import CaptureSet from '../../../components/CaptureSet.astro';
import { sitePath } from '../../../lib/links';

**07 / A little chaos.** Turn up Chaos to make the machines request many sounds. The counters show which requests are accepted, which replace an older play, and which are refused by the event’s limits.

These are real Unity captures with the scene’s own models, camera, and controls. Select a picture to enlarge it, or open **Try this in Unity** below it for the steps. The graph close views explain individual parts of the full graph.

<a href={sitePath('captures/')}>All screenshot galleries</a> ·
<a href={sitePath('guides/results-and-limits/')}>Read how playback results and limits work</a>

Read the <a href={sitePath('demos/07-arcade/')}>complete Arcade walkthrough</a> for controls, graph flow, and code.

## In the scene

<CaptureSet demo="07-arcade" group="game" />

## In the graph and Inspector

A graph connects blocks called **nodes** to control a sound. The Inspector holds the event’s playback settings. The pictures below show the saved graph and settings; a full graph is an overview, so use the close views to read smaller fields.

<CaptureSet demo="07-arcade" group="editor" />
