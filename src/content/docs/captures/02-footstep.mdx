---
title: 'Footstep in pictures.'
description: 'Unity screenshots of Footstep, with scene states, sound graphs, and steps to try.'
kind: guide
sourceRevision: 746a1cc7d2602bc59df6096eac50508ded6ef49e
sourcePaths:
  - Assets/DEV505/SoundManager/Samples/02-Footstep/Demo.unity
  - Assets/DEV505/SoundManager/Scripts/Samples/Footstep/FootstepAudio.cs
tags: [screenshots, demos, footstep]
---

import CaptureSet from '../../../components/CaptureSet.astro';
import { sitePath } from '../../../lib/links';

**02 / Every step counts.** Walk across three surfaces and compare the footstep readout. One event key chooses grass, stone, or wood through a parameter; each surface has several short variations cut from a longer recording.

These are real Unity captures with the scene’s own models, camera, and controls. Select a picture to enlarge it, or open **Try this in Unity** below it for the steps. The graph close views explain individual parts of the full graph.

<a href={sitePath('captures/')}>All screenshot galleries</a> · <a href={sitePath('guides/parameters/')}>Read how parameters change a sound</a>

## In the scene

<CaptureSet demo="02-footstep" group="game" />

## In the graph and Inspector

A graph connects blocks called **nodes** to control a sound. The Inspector holds the event’s playback settings. The pictures below show the saved graph and settings; a full graph is an overview, so use the close views to read smaller fields.

<CaptureSet demo="02-footstep" group="editor" />
