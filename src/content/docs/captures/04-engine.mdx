---
title: 'Engine in pictures.'
description: 'Unity screenshots of Engine, with scene states, sound graphs, and steps to try.'
kind: guide
sourceRevision: 746a1cc7d2602bc59df6096eac50508ded6ef49e
sourcePaths:
  - Assets/DEV505/SoundManager/Samples/04-Engine/Demo.unity
  - Assets/DEV505/SoundManager/Scripts/Samples/Engine/EngineDriveDemo.cs
tags: [screenshots, demos, engine]
---

import CaptureSet from '../../../components/CaptureSet.astro';
import { sitePath } from '../../../lib/links';

**04 / A little drive.** Start the engine, raise the throttle, then try a horn or gear change. The running sound uses layers whose speed and loudness change with the demo’s RPM and load values.

These are real Unity captures with the scene’s own models, camera, and controls. Select a picture to enlarge it, or open **Try this in Unity** below it for the steps. The graph close views explain individual parts of the full graph.

<a href={sitePath('captures/')}>All screenshot galleries</a> ·
<a href={sitePath('guides/signals-and-notifications/')}>Read how signals trigger another action</a>

Read the <a href={sitePath('demos/04-engine/')}>complete Engine walkthrough</a> for controls, graph flow, and code.

## In the scene

<CaptureSet demo="04-engine" group="game" />

## In the graph and Inspector

A graph connects blocks called **nodes** to control a sound. The Inspector holds the event’s playback settings. The pictures below show the saved graph and settings; a full graph is an overview, so use the close views to read smaller fields.

<CaptureSet demo="04-engine" group="editor" />
