---
title: 'Weather in pictures.'
description: 'Unity screenshots of Weather, with scene states, sound graphs, and steps to try.'
kind: guide
sourceRevision: 746a1cc7d2602bc59df6096eac50508ded6ef49e
sourcePaths:
  - Assets/DEV505/SoundManager/Samples/05-Weather/Demo.unity
  - Assets/DEV505/SoundManager/Scripts/Samples/Weather/WeatherDemo.cs
tags: [screenshots, demos, weather]
---

import CaptureSet from '../../../components/CaptureSet.astro';
import { sitePath } from '../../../lib/links';

**05 / A little rain.** Move from clear skies to a storm. Rain and wind use continuous layers; each lightning strike starts a separate thunder event at the strike position, with a short delay before the rumble.

These are real Unity captures with the scene’s own models, camera, and controls. Select a picture to enlarge it, or open **Try this in Unity** below it for the steps. The graph close views explain individual parts of the full graph.

<a href={sitePath('captures/')}>All screenshot galleries</a> ·
<a href={sitePath('guides/parameters/')}>Read how to change sound parameters</a>

Read the <a href={sitePath('demos/05-weather/')}>complete Weather walkthrough</a> for controls, graph flow, and code.

## In the scene

<CaptureSet demo="05-weather" group="game" />

## In the graph and Inspector

A graph connects blocks called **nodes** to control a sound. The Inspector holds the event’s playback settings. The pictures below show the saved graph and settings; a full graph is an overview, so use the close views to read smaller fields.

<CaptureSet demo="05-weather" group="editor" />
