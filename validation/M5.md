# M5 — Graph reference, advanced API, and troubleshooting

Completed locally on 20 September 2026. Unity source revision: `746a1cc7d2602bc59df6096eac50508ded6ef49e`, Unity 6000.6.0f1. No deployment was performed.

## Delivered

- Seventeen new authored pages: ten graph pages, five advanced pages, and two guides for outcomes/troubleshooting. Total: 76 authored pages, 77 built HTML pages including 404.
- All 49 shipped operations in six families, with source-derived fixed ports, numbered-port conventions, authored defaults/options, runtime behavior, and practical examples. Each node is individually linked in its page's table of contents.
- Trimming and preview instructions with a real Unity range capture; multiple ranges from one recording, baked clip identity, preparation, and cleanup are explained.
- Execution order, Sequence versus elapsed-time flow, release paths, natural completion versus cancellation, conditions, repeats, waits, validation, and debugging limits.
- Complete advanced integration coverage for 18 types, including five promoted observation/trace types. Every declared advanced member has a rendered signature target and explanation; no consumer/generated/advanced member remains deferred. Compiler/editor infrastructure remains explicitly excluded from the supported integration reference.
- Registration, preparation, duplicate-key handling, custom clocks/backends, stale voice handles, parameter declarations, current revision observation, and ring-buffer trace reading.
- All twelve play statuses and five notification kinds, plus practical troubleshooting and the warning that runtime debug UI/inspection can reduce FPS. No universal performance claim is made.
- Cross-links to existing complete recipes and demos. Existing 84 authentic Unity captures and eleven verified complete scripts are retained. Private source repository links remain prohibited by build validation.

## Source and Unity verification

`m5/source-audit.json` records the source files/hashes reviewed and exact hashes of the three new C# excerpts. `m5/CompileM5Excerpts.cs.txt` contains those excerpts wrapped in the required fields/methods; all compile in the current Unity project with no compiler diagnostics. These are excerpts, not additional ready-to-attach components.

`m5/VerifyM5Reference.cs.txt` runs in the live Unity Editor through CLI `run_script`, compiled in memory outside Assets. The command envelope and successful inner execution result are retained in `m5/runtime-command.json`; `m5/runtime-audit.json` records **180 passed checks** and the native 49-node catalogue with its fixed ports.

Checks cover operation completeness and port resolution, zero/one-based numbered-port boundaries, registration/key lookup, preparation and queued starts, delayed flow and pause, End Event cancellation, natural completion, silent Keep Alive, unregistering, duplicate keys, stale handles, revision/voice/node observation, trace wraparound/cursors, loading timeout, missing-clip readiness, initial typed parameters, the actual Footstep graph's terminal kind, parameter acceptance/clamping, catalogue deduplication, service disposal/session identity, and built-in backend pool construction.

The control-layer checks use a deliberately small fake backend and controllable clock. They verify scheduling/ownership outcomes, not audible mixing, real fade envelopes, or timing accuracy of a physical audio device. The built-in backend is constructed and inspected in a temporary preview scene. This milestone makes no new audio-quality or FPS benchmark claim.

Unity was opened for this audit. Both recorded snapshots show the Launch scene (`10-Launch/Demo.unity`), clean, not playing, not paused, and background running false. Temporary ScriptableObjects/services and the preview scene are cleaned in `finally`; no demo was edited or saved. Earlier Play Mode evidence for the eleven complete scripts remains in M1–M4.

## Website verification

- `npm run validate`: Astro reports zero errors/warnings/hints. Content checks pass for 76 authored pages, ten demos, 84 captures, eleven verified complete scripts, and 867 inventoried member declarations.
- The M5 guard checks all 49 nodes against native ports, all 18 advanced type references, exact excerpt hashes, successful runtime evidence, and unchanged Unity scene state.
- Production build: **77 HTML pages**, **8,359 local links/assets**, **64 API coverage targets**, repository base path and Pagefind output verified. No private Unity repository link is emitted.
- `npm run test:browser`: **20 passed**, including four new M5 tests for all node tables and table-of-contents navigation, advanced coverage/code copying, search, narrow phone layouts, and the trim screenshot dialog. Previous screenshot-preview regression checks still pass.
- Desktop/phone visual review: `m5/flow-desktop.png`, `m5/service-desktop.png`, `m5/troubleshooting-phone.png`, and `m5/trimming-phone.png`. Historical milestone screenshots were retained rather than overwritten by this run. Latest machine results remain in `browser-results.json`.

M6's complete release review and M7's handoff remain open. This milestone does not claim those stages or publish the local changes.
