# M2 validation — 19 September 2026

Completed against Unity source revision `746a1cc7d2602bc59df6096eac50508ded6ef49e` in Unity `6000.6.0f1`. The source revision is unchanged from M1. This milestone is validated locally; it does not claim a new GitHub Pages publication.

## Delivered

- Twenty new pages, bringing the site to 27 authored pages plus its generated 404 page.
- A reading path and eight guides covering registration, libraries, generated keys, a first graph, events/plays/voices, parameters, positioning/ownership, stop/pause/cleanup, signals/notifications, and result/limit troubleshooting. The reading path is one of those nine guide-directory pages.
- Eleven new API chapters covering context, parameter types, results/handles, ownership, notifications, services, requests, components, event keys/assets, settings, and reference coverage. The existing API overview and SoundBus reference were revised.
- Beginner revisions to the first-sound guide, Campfire recipe/walkthrough, and home copy. Setup explains Unity windows, exact asset/menu locations, script placement, expected sounds, and common failures.
- Four complete example components: the existing Campfire lifecycle example plus FirstSound, MovingBeeSound, and EngineSoundControls. Their pages import the verified files directly.
- A Roslyn inventory of 163 public types and 867 member declarations, including partial, generated, and editor declarations. Classification: 35 everyday integration types, 13 generated types, 13 advanced types, and 102 infrastructure types. Explicitly public declarations are counted; inherited Unity/System members and private containing types are excluded.
- Source-derived signature blocks, per-member coverage records, and build checks for reference targets. Advanced members remain visibly deferred; implementation members have explicit scope exclusions. Five compiled-graph members on SoundEvent are also classified as infrastructure.

## Website verification

`npm run validate` passes using Node 24.21.0:

- Astro/TypeScript: zero errors, warnings, or hints.
- Content records: 27 pages, ten demo summaries, four unchanged authentic Unity captures, four verified C# components, and all 867 inventoried member declarations accounted for.
- Static build: 28 HTML pages; 1,744 local links/assets and 46 distinct documented API targets checked under `/Sound-Manager-Doc/`.
- Pagefind search index and repository-relative assets generated successfully.

The production build still emits framework bundler warnings about the MDX `use astro:head-inject` directive, along with Starlight's empty optional collection/404 notices. These are separate from the clean Astro diagnostics. The built pages, styling, links, and search passed the browser checks; the build is not described as warning-free.

`npm run test:browser` passes all eight checks:

1. Home, source imagery, ten-card gallery, navigation, and client errors.
2. Direct Campfire route/refresh, screenshots, enlarged graph, Escape dismissal, and restored focus.
3. Production search for public API and sound concepts.
4. Campfire clipboard content exactly matches its verified component.
5. Existing pages at 390 and 320 CSS px and the expanded mobile API menu.
6. M2 guide-to-reference navigation, source-derived signatures, direct reference anchors, and coverage links.
7. Exact clipboard copies of all three new components and search for context/ownership topics.
8. Nine M2 guide/reference pages at 320 CSS px without page-wide horizontal overflow.

Desktop and phone captures were visually reviewed. The existing cream/forest theme, code styling, and responsive navigation remain consistent. Screenshots include `m2-moving-sound-desktop.png` and `m2-first-sound-mobile.png`; the machine-readable browser report is `browser-results.json`.

## Unity verification

All four complete components compiled together in memory with the M2 harness, with no compiler diagnostics. The exact source hashes and evidence links are in `examples.json`. The unchanged Campfire component retains M1's eight runtime checks and was recompiled for M2. Short, labeled excerpts were reviewed against the current source contracts; they are not claimed as standalone compiled components.

The M2 Play Mode harness passed 42 checks. It exercised the three new components and focused API behavior:

- Automatic playback, repeated-start guards, stop/release, stale handles, and disable cleanup.
- Actual Bee AudioSource movement after its target Transform moved, initial/live WingSpeed, and object-wide stopping with an explicit shared owner.
- Engine RPM, a Horn signal adding a voice to the existing play, per-play/global pause interaction, matching terminal notifications, and subscription disposal.
- Following without an owner, destroyed-target Stop, and a temporary Detach event continuing at its last position.
- Wrong typed parameters rejected, all override layers applied in order, initial values immediate, caller arrays not retained, discarded With return values, the four-entry limit, and five typed initial values.
- Queued admission before preparation, end-of-update notification delivery, RejectNew concurrency, and a finished handle remaining structurally valid while no longer alive.

See `m2-unity-runtime.json` and `VerifyM2Examples.cs.txt`. Temporary fixtures used the current runtime and Unity audio backend; they were not saved as assets. The test checks state and actual AudioSource positions/voices, not subjective listening quality or DSP timing guarantees.

Source review also clarified that Ready means no catalogue entries are still loading, not that every event has valid clips; Following does not assign an owner implicitly; owner release applies loss policy on a later update; and custom stop-node fades are distinct from the API's fallback fade.

## Unity session preservation

After testing, `Assets/DEV505/SoundManager/Samples/01-Campfire/Demo.unity` was verified clean and outside Play Mode, with background playback restored to false. A later read-only check found `10-Launch/Demo.unity` open, also clean and outside Play Mode with background playback false; this documentation task did not switch that scene back. Runtime registrations, temporary objects, and in-memory event fixtures were cleaned up. No Unity scene or asset was saved or edited by this task; the source worktree has the same pre-existing changes recorded in the baseline.

## Remaining scope

M3–M7 remain open: the other nine demos' authentic captures and detailed walkthroughs, the complete graph catalogue, advanced service/backend/clock material, full-site accessibility/performance review, and publication of the completed documentation release. M2 introduces setup examples for Bee and Engine without claiming those demos' full walkthroughs or new screenshots are complete.
