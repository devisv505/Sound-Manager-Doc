# M4 validation — detailed demos and practical recipes

Completed locally on 19–20 September 2026. This milestone does not publish or push the website.

## Delivered scope

All ten demo walkthroughs are available from the home gallery and sidebar, with previous/next navigation. Each uses the exact demo title, a plain-language “What this demo shows” panel, scene-opening instructions, verified controls, listening steps, graph explanations, parameter/signal information, code excerpts, common surprises, and source/API/recipe links. The pages explicitly distinguish Sound Manager behavior from sample gameplay and visual effects.

The twelve recipe topics in the specification are complete. They reuse eleven complete C# components; seven components are new in M4. Recipes state the filename, where it belongs, manager/event/key/listener prerequisites, Inspector assignments, test actions, expected behavior, and cleanup. Footsteps finish naturally; longer interactions retain handles or owners. Copyable complete code is imported directly from the checked files.

M4 reuses M3's 84 authentic Unity captures. No models, Unity scene assets, audio, or screenshots were changed for this milestone. Footstep remains correctly described as 2D in its saved graph, Workshop uses a second play for taps over a tool, Jukebox uses graph-smoothed current gains, and Portals destroys sound-rig GameObjects within one scene. Launch distinguishes its initial graph gate from the controller's continuing safety checks and starts flight from a matching natural Finished notification.

## Source and graph evidence

- Unity: **6000.6.0f1**.
- Source revision: **746a1cc7d2602bc59df6096eac50508ded6ef49e**, unchanged from M3.
- [Native graph audit](m4/source-audit.json): all 22 sample events, settings, parameter definitions, signals, nodes, and edges.
- [Source hashes](m4/source-files.json): 58 files, including graph importer metadata where event settings are saved, demo controllers, input bindings, and Arcade's six-voice settings.
- [Graph audit helper](m4/AuditGraphs.cs.txt): in-memory Unity Editor script, kept outside Assets.
- [Published settings](../content-data/demo-audit.json): normalized from the audit by `npm run audit:demos`. The content checker rejects changes that no longer match it.

The native audit is the evidence for graph parameters/settings. Controller code and input bindings supply interaction details. Stored revision/hash records describe the checked snapshot; they do not automatically verify a future Unity project revision.

## Unity code verification

All eleven complete examples compile together in the running Editor, in memory, without diagnostics. See [compilation record](m4/compilation.json), [full CLI result](m4/runtime-cli.json), and [example hashes](examples.json).

The [M4 runtime harness](m4/VerifyM4Examples.cs.txt) executes the seven new components in a temporary Campfire Play Mode session. All **76 checks** pass, including preparation/registration and these behavior checks:

- Footstep's scene-to-graph integer mapping for all three surfaces, short natural endings, and ignored unknown ground.
- Weather's live smoothed Rain target, initially silent thunder Delay, eventual spatial voice at the strike point, and owner cleanup cancelling a second pending thunder.
- Workshop's main tool session and separate overlapping hammer play, tool update before Work, silent Keep Alive sessions after the Pause signal, and disable cleanup.
- Arcade's accepted fourth zap replacing the first, replacement notification, refused third coin, jackpot cooldown, and cleanup. These tests use the actual graphs; they do not claim to benchmark a saturated backend or prove cross-event stealing.
- Jukebox initial Gain zero, fade-in, two simultaneous voices during handoff, pause on both tracks, frozen current gain while paused, resumed fade, outgoing retirement, and final cleanup.
- Portal Detach surviving owner destruction at its last position, Stop ending the comparison play, and cleanup of detached handles.
- Launch's silent unmet-condition wait, cancellation without flight, satisfied-condition countdown, natural Finished triggering flight, following the moving rocket, global pause, and disable cleanup.
- Notification subscriptions return to their prior count after the example components are disabled.

The four earlier components retain their unchanged M1/M2 runtime evidence and are recompiled with M4. Short excerpts are explicitly labeled and reviewed against the checked implementations/API; they are not represented as standalone Unity components. These checks verify state, actual AudioSource voices/positions, and graph lifetime. They do not establish subjective mix quality, exact DSP timing, or beat synchronization.

## Website verification

`npm run validate` passes with Node 24.21.0:

- Astro/TypeScript: **zero errors, warnings, or hints**.
- Content: **59 authored pages**, ten available demos, twelve recipe topics, 84 capture records, eleven verified examples, and 867 API declarations accounted for.
- M4 checks: 22 event records match the native audit; all recipes reference checked files; demo sections/images/settings and scene restoration records are complete.
- Production output: **60 HTML pages**, **5,537 local links/assets**, 46 API targets, repository base path, and Pagefind output verified.

The existing framework notices about MDX `use astro:head-inject` and an empty optional i18n collection may appear during bundling. They do not fail the build and are separate from the clean Astro diagnostics. No framework/package migration is included in this content milestone.

`npm run test:browser` passes **all 15 checks**. The M4 additions visit all demo guides and check their event/settings content, load their responsive images, follow previous/next navigation, visit every recipe, and copy each complete component exactly. All ten guides and twelve recipes fit a 320 px viewport without page-wide overflow. Search finds hammer-overlap and launch-clearance explanations. The earlier API, gallery, enlargement, keyboard, and mobile checks continue to pass.

Reviewed screenshots: [Workshop desktop](m4/workshop-desktop.png), [recipe catalogue](m4/recipes-desktop.png), [Jukebox settings on a phone](m4/jukebox-settings-mobile.png), and [Launch on a phone](m4/launch-mobile.png). The existing cream, sage, dark-green, and voxel brand treatment is preserved. A first browser run contained one incorrect test assumption requiring OnDisable in every example; the naturally ending Footstep recipe correctly needs none. The assertion was corrected, and the complete final run passes.

## Cleanup and remaining milestones

The [before](m4/session-before.json) and [after](m4/session-after.json) records match: clean `10-Launch/Demo.unity`, Play Mode off, Pause off, background running off. Temporary components, owners/plays, subscriptions, and registrations are cleaned up. No Unity scene or asset was saved, and the Unity worktree retains its pre-existing staged/deleted GraphWorkflow placeholder and `.DS_Store` files unchanged.

The site remains a static GitHub Pages build with no Unity installation, paid backend, audio files, or API key needed to build/browse it. M5–M7 remain open for graph/advanced reference, full-site quality review, and release work.
